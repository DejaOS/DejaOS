interface:
  display_name: "DejaOS SDK 2.0 App Dev"
  short_description: "Create, run, debug, and review DejaOS SDK 2.0 apps."
  default_prompt: "Use $dejaos-app-dev-sdk2-0 to create or modify a DejaOS SDK 2.0 app with dejaos CLI, then sync it to the correct USB device, run it, and inspect the logs."
