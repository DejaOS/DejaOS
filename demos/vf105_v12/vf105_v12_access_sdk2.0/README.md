# vf105_v12_access_sdk2.0 Access Control Application Source Code

This directory contains the JavaScript source code for the `vf105_v12_access_sdk2.0` access control application.

**Important Note:**

The JavaScript source code in this project is identical to the one used in the `vf203_v12_access_sdk2.0` project (located at `../../vf203_v12/vf203_v12_access_sdk2.0`).

The only difference between the two projects lies in the underlying native shared library (`.so`) files. The specific version of the `.so` files to be used is determined and configured by the `app.dxproj` project file.
